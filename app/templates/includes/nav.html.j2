<nav class="navbar  navbar-light bg-light">
    {# <div class="row d-inline-block">   #}
        <div class="row w-100">
            <div class="col-1">
                <a class="navbar-brand" href=""> 
                    <img src="{{url_for('static', filename='images/pokemon_madness_label.webp')}}" style="height: 5em"> 
                </a>
            </div>
        {# <button class="navbar-toggler d-inline-block" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button> #}
            <div class="col-9" id="navbarNavAltMarkup">
                
                    {% if current_user.is_authenticated %}
                    <a class="nav-link text-dark d-inline-block" href="{{url_for('main.index')}}">Home</a>
                    <a class="nav-link text-dark d-inline-block" href="{{url_for('main.pokemon')}}">Training Grounds</a>
                    <a class="nav-link text-dark d-inline-block" href="{{url_for('main.pokemon_team')}}">Pokemon Team</a>
                    <a class="nav-link text-dark d-inline-block" href="{{url_for('main.pokemon_battle')}}">Pokemon Battle</a>
                    {% else %}
                    <a class="nav-link d-inline-block" href="{{url_for('auth.login')}}">Login</a>
                    <a class="nav-link d-inline-block" href="{{url_for('auth.register')}}">Register</a>
                    {% endif %}
            </div>
        
                {% if current_user.is_authenticated %}
            <div class="col-2"  >
                <div class="row w-100">
                    <div class="col-8"></div>
                    <div class="col-2 pt-4">
                        <a href="">
                            <img  class="align-middle" src="{{current_user.get_icon_url()}}" style="height: 40px;">
                        </a>
                    </div>
                    <div class="col-1 align-middle">
                        <a class="nav-link text-dark" href="{{url_for('auth.edit_profile')}}"><i class="bi bi-gear-fill"></i></a>


                        <a class="nav-link text-dark" href="{{url_for('auth.logout')}}"><i class="bi bi-door-closed"></i></a>

                    </div>

                </div>
              
            </div>

            {% endif %}
            
       
    {# </div> #}
</nav>